sc(p.title)}</h3></div>`).join('')}</div></div></div></section>
 <section class="institutional"><div class="wrap"><div class="eyebrow" style="color:#aebfba">Desde 1939</div><h2>Há mais de oito décadas, ajudamos histórias a encontrar leitores.</h2><p style="max-width:710px;line-height:1.6;color:#d5dedb">O Grupo Ediouro reúne diferentes casas editoriais, leitores e maneiras de descobrir livros.</p><div class="newsletter"><h3>Quer um livro novo na sua vida?</h3><div><div class="field">seu@email.com</div><div class="cta">Quero receber</div></div></div></div></section>
 </main>`;
}

function catalog(params){
 let q=(params.get('q')||'').trim().toLowerCase(), marca=params.get('marca')||'', serie=params.get('serie')||'', categoria=params.get('categoria')||'', list=[...DATA.works];
 if(marca)list=list.filter(w=>w.imprint===marca);
 if(serie)list=list.filter(w=>w.series===serie);
 if(categoria)list=list.filter(w=>(w.categories||[]).includes(categoria));
 if(q)list=list.filter(w=>[w.title,w.shortDescription,authorNames(w),w.series?S[w.series]?.name:'',...(ED[w.slug]||[]).flatMap(e=>[e.isbn,e.ean])].join(' ').toLowerCase().includes(q));
 return `<main><div class="page-hero"><div class="wrap"><div class="eyebrow">Catálogo</div><h1>Livros</h1><p>${q?`Resultados para “${esc(q)}”`:'85 obras reais já conectadas nesta primeira carga.'}</p></div></div><section><div class="wrap"><div class="catalog-tools"><span>${list.length} títulos</span><span class="link" onclick="go('/busca')">Buscar no catálogo</span></div><div class="catalog-grid">${list.map(bookCard).join('')}</div></div></section></main>`;
}
function retailerUrl(name,w,e){const q=encodeURIComponent((e?.isbn||w.title)+' '+w.title);if(name==='Amazon')return 'https://www.amazon.com.br/s?k='+q;if(name==='Mercado Livre')return 'https://lista.mercadolivre.com.br/'+encodeURIComponent(w.title);if(name==='Travessa')return 'https://www.travessa.com.br/Busca.aspx?d=1&texto='+q;return 'https://www.martinsfontespaulista.com.br/busca?q='+q}
function bookPage(slug,params){
 const w=W[slug];if(!w)return notFound();
 const eds=ED[slug]||[];let ed=eds.find(e=>e.id===params.get('edicao'))||principal(w)||eds[0]||{};
 const auts=authorSlugs(w).map(s=>C[s]).filter(Boolean), rel=DATA.works.filter(x=>x.slug!==slug&&(x.imprint===w.imprint||x.categories?.some(c=>w.categories?.includes(c)))).slice(0,5);
 const im=I[w.imprint]||I.trama, ser=w.series?S[w.series]:null;
 return `<main><section><div class="wrap"><div class="breadcrumb">Livros / ${esc(im.name)}${ser?' / '+esc(ser.name):''} / ${esc(w.title)}</div><div class="detail-top"><div>${coverForEdition(w,ed)}</div><div class="detail-main"><div class="eyebrow" style="color:${im.color}">■ ${esc(im.name)}</div><h1>${esc(w.title)}</h1><p class="detail-author">${esc(authorNames(w))}</p><p class="detail-pitch">${esc(w.shortDescription||'')}</p><div class="edition-label">ESCOLHA A EDIÇÃO</div><div class="editions">${eds.map((e,i)=>`<button class="ed ${e.id===ed.id?'active':''}" onclick="selectEdition('${slug}','${e.id}')">${esc(e.label||e.format||'Edição')}<small>${money(e.price)}</small></button>`).join('')}</div><div class="price"><span style="display:block;font:700 9px/1.2 'Archivo',Arial,sans-serif;letter-spacing:.14em;text-transform:uppercase;color:#7c736b;margin-bottom:5px">Preço sugerido</span>${money(ed.price)||'—'}</div><div class="retail-title">ONDE COMPRAR</div><div class="retail-grid">${['Amazon','Mercado Livre','Travessa','Martins Fontes'].map(n=>`<div class="retail" onclick="window.open('${retailerUrl(n,w,ed)}','_blank')">${esc(n)} <span>↗</span></div>`).join('')}</div><div class="meta-grid"><div><div class="meta-row"><span>Formato</span><strong>${esc(ed.format||'—')}</strong></div><div class="meta-row"><span>Páginas</span><strong>${esc(ed.pageCount||'—')}</strong></div><div class="meta-row"><span>ISBN</span><strong>${esc(ed.isbn||'—')}</strong></div></div><div><div class="meta-row"><span>Acabamento</span><strong>${esc(ed.binding||'—')}</strong></div><div class="meta-row"><span>Dimensões</span><strong>${esc(ed.dimensions||'—')}</strong></div><div class="meta-row"><span>Status</span><strong>${esc(ed.status||'—')}</strong></div><div class="meta-row"><span>EAN</span><strong>${esc(ed.ean||'—')}</strong></div></div></div></div></div>
 <div class="detail-block"><h2>Sobre o livro</h2><div class="bodycopy">${(w.description||[]).map(p=>`<p>${esc(p)}</p>`).join('')}</div><div class="tags">${(w.categories||[]).map(c=>`<span class="tag">${esc(c.replaceAll('-',' '))}</span>`).join('')}</div></div>
 ${ser&&ser.workSlugs.length>1?`<div class="detail-block"><div class="sec-head"><h2>${esc(ser.name)} · ordem de leitura</h2><div class="link" onclick="go('/series/${ser.slug}')">Sobre a série</div></div><div class="volume-list">${ser.workSlugs.map((s,i)=>W[s]?`<div class="volume" onclick="go('/livros/${s}')">${cover(W[s])}<div><div class="eyebrow">Volume ${i+1}${s===slug?' · você está aqui':''}</div><h3>${esc(W[s].title)}</h3></div></div>`:'').join('')}</div></div>`:''}
 ${auts.map(a=>`<div class="detail-block"><h2>${esc(a.name)}</h2><p style="max-width:650px;line-height:1.6;color:#6e665e">${esc(a.shortBio||a.bio?.[0]||'Autor publicado pelo Grupo Ediouro.')}</p><div class="link" onclick="go('/autores/${a.slug}')">Todos os livros de ${esc(a.name.split(' ')[0])}</div></div>`).join('')}
 </div></section><section class="related"><div class="wrap"><div class="sec-head"><h2>Você também pode gostar</h2></div><div class="shelf">${rel.map(bookCard).join('')}</div></div></section>
 <section><div class="wrap"><div class="sec-head"><h2>Conteúdos relacionados</h2></div>${POSTS.filter(p=>p.relatedWorks.includes(slug)).map(p=>`<div class="story" onclick="go('/descubra/${p.slug}')"><div class="eyebrow">${esc(p.kind)}</div><h3>${esc(p.title)}</h3><p>${esc(p.standfirst)}</p></div>`).join('')||'<p style="color:#6d655d">Ainda não há conteúdos editoriais relacionados a este título nesta preview.</p>'}</div></section></main>`;
}
function coverForEdition(w,e){const url=e?.cover||coverUrl(w);return url?`<div class="cover"><img src="${esc(url)}" alt="Capa de ${esc(w.title)}"></div>`:cover(w)}
function selectEdition(slug,id){go('/livros/'+slug+'?edicao='+encodeURIComponent(id))}
function seriesList(){return `<main><div class="page-hero"><div class="wrap"><div class="eyebrow">Séries e universos</div><h1>Histórias grandes demais para um livro só.</h1><p>Ordens de leitura reais para as séries já presentes no catálogo.</p></div></div><section><div class="wrap"><div class="series-list">${DATA.series.map(s=>`<article class="series-card" onclick="go('/series/${s.slug}')"><div class="series-covers">${s.workSlugs.slice(0,3).map(x=>W[x]?cover(W[x]):'').join('')}</div><h3>${esc(s.name)}</h3><p>${esc(s.description)}</p><div class="eyebrow">${s.workSlugs.length} volumes integrados</div></article>`).join('')}</div></div></section></main>`}
function seriesPage(slug){const s=S[slug];if(!s)return notFound();const im=I[s.imprint]||I.trama,a=s.mainContributor?C[s.mainContributor]:null;return `<main><div class="series-hero" style="background:${im.color};color:${im.ink}"><div class="wrap"><div class="eyebrow" style="color:inherit;opacity:.7">SÉRIE</div><h1>${esc(s.name)}</h1><p style="font-size:17px;line-height:1.6;max-width:760px;opacity:.85">${esc(s.description)}</p>${a?`<div class="link" style="color:inherit;border-color:currentColor;display:inline-block;margin-top:18px" onclick="go('/autores/${a.slug}')">Por ${esc(a.name)}</div>`:''}</div></div><section><div class="wrap"><div class="sec-head"><h2>Ordem de leitura</h2></div><div class="volume-list">${s.workSlugs.map((x,i)=>W[x]?`<div class="volume" onclick="go('/livros/${x}')">${cover(W[x])}<div><div class="eyebrow">Volume ${i+1}</div><h3>${esc(W[x].title)}</h3><p>${esc(W[x].shortDescription||'')}</p></div></div>`:'').join('')}</div></div></section></main>`}
function brandsList(){return `<main><div class="page-hero"><div class="wrap"><div class="eyebrow">Marcas editoriais</div><h1>Muitas vozes. Muitos leitores. Uma Ediouro.</h1><p>Cada marca publica para um leitor diferente — e todas dividem a mesma casa.</p></div></div><section><div class="wrap"><div class="brand-grid">${IMPRINTS.map(im=>{const books=DATA.works.filter(w=>w.imprint===im.slug).slice(0,3);return `<article class="brand-card" style="background:${im.color};color:${im.ink}" onclick="go('/marcas/${im.slug}')"><div><div class="eyebrow" style="color:inherit;opacity:.7">${esc(im.name)}</div><h2>${esc(im.tagline)}</h2><p>${esc(im.description)}</p></div><div class="mini-row">${books.map(w=>cover(w)).join('')}</div></article>`}).join('')}</div></div></section></main>`}
function brandPage(slug){const im=I[slug];if(!im)return notFound();const books=DATA.works.filter(w=>w.imprint===slug);return `<main><div class="brand-hero" style="background:${im.color};color:${im.ink}"><div class="wrap"><div class="eyebrow" style="color:inherit;opacity:.7">${esc(im.name).toUpperCase()}</div><h1>${esc(im.tagline)}</h1><p>${esc(im.description)}</p><div class="brand-meta">${im.founded?`<span>Desde ${im.founded}</span>`:''}<span>${books.length?books.length+' títulos integrados':'Catálogo em integração'}</span><span>${esc(im.focus.join(' · '))}</span></div></div></div>${books.length?`<section><div class="wrap"><div class="sec-head"><h2>Destaques da ${esc(im.name)}</h2><div class="link" onclick="go('/livros?marca=${im.slug}')">Ver no catálogo</div></div><div class="shelf">${books.slice(0,8).map(bookCard).join('')}</div><div class="sec-head" style="margin-top:55px"><h2>Catálogo completo</h2></div><div class="catalog-grid">${books.map(bookCard).join('')}</div></div></section>`:`<section><div class="wrap"><div class="notice"><strong>CATÁLOGO EM INTEGRAÇÃO.</strong> A casa editorial já está nesta arquitetura; os livros entram na próxima carga oficial.</div></div></section>`}</main>`}
function authorsList(){const list=DATA.contributors.filter(c=>c.roles?.includes('autor')).sort((a,b)=>a.name.localeCompare(b.name,'pt-BR'));return `<main><div class="page-hero"><div class="wrap"><div class="eyebrow">Pessoas</div><h1>Autores</h1><p>Autores já presentes no catálogo real de Trama e Livros da Alice.</p></div></div><section><div class="wrap"><div class="authors">${list.map(a=>`<article class="author" onclick="go('/autores/${a.slug}')"><div class="monogram">${initials(a.name)}</div><h3>${esc(a.name)}</h3><p>${esc(a.shortBio||'Autor publicado pelo Grupo Ediouro.')}</p></article>`).join('')}</div></div></section></main>`}
function authorPage(slug){const a=C[slug];if(!a)return notFound();const books=DATA.works.filter(w=>authorSlugs(w).includes(slug));return `<main><div class="page-hero"><div class="wrap"><div class="eyebrow">Autor</div><h1>${esc(a.name)}</h1><p>${esc(a.shortBio||a.bio?.[0]||'Autor publicado pelo Grupo Ediouro.')}</p></div></div><section><div class="wrap"><div class="catalog-grid">${books.map(bookCard).join('')}</div></div></section></main>`}
function discoverList(){return `<main><div class="page-hero"><div class="wrap"><div class="eyebrow">Descubra</div><h1>Caminhos de leitura.</h1><p>Guias, curadorias e bastidores. Conteúdo editorial de demonstração nesta preview, sempre ligado a livros reais do catálogo.</p></div></div><section><div class="wrap"><div class="discover-grid"><article class="post-feature" onclick="go('/descubra/${POSTS[0].slug}')"><div class="eyebrow">${esc(POSTS[0].kind)}</div><h2>${esc(POSTS[0].title)}</h2><p>${esc(POSTS[0].standfirst)}</p><div class="link">Ler conteúdo</div></article><div class="post-list">${POSTS.slice(1).map(p=>`<article class="story" onclick="go('/descubra/${p.slug}')"><div class="eyebrow">${esc(p.kind)}</div><h3>${esc(p.title)}</h3><p>${esc(p.standfirst)}</p></article>`).join('')}</div></div></div></section></main>`}
function articlePage(slug){const p=P[slug];if(!p)return notFound();const related=p.relatedWorks.map(s=>W[s]).filter(Boolean);const im=p.relatedImprint?I[p.relatedImprint]:null;return `<main><section><div class="narrow article"><div