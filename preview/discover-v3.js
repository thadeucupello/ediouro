function discoverPostBooks(p){return (p.relatedWorks||[]).map(s=>W[s]).filter(Boolean)}
function discoverPostBrand(p){return p.relatedImprint?I[p.relatedImprint]:null}
function discoverPostVisual(p,limit=3){const books=discoverPostBooks(p).slice(0,limit);return books.length?`<div class="discover-visual">${books.map(w=>cover(w)).join('')}</div>`:`<div class="discover-visual"><div class="cover placeholder" style="background:var(--ediouro-blue,#0200BB)"><strong>Descubra</strong><small>Ediouro</small></div></div>`}
function discoverMeta(p){const d=new Date(p.publishedAt+'T12:00:00').toLocaleDateString('pt-BR',{day:'2-digit',month:'long',year:'numeric'});return `${d} · ${p.readingTime} min de leitura`}
window.discoverFilter=function(kind){document.querySelectorAll('.discover-filter').forEach(b=>b.classList.toggle('active',b.dataset.kind===kind));document.querySelectorAll('.discover-all-card').forEach(c=>{c.classList.toggle('is-hidden',kind!=='todos'&&c.dataset.kind!==kind)})}
function discoverList(){const featured=POSTS[0],rest=POSTS.slice(1),kinds=[...new Set(POSTS.map(p=>p.kind))];return `<main><div class="discover-v3-hero"><div class="wrap"><div class="eyebrow">Descubra</div><h1>Caminhos de leitura.</h1><p>Guias, curadorias e bastidores para entrar mais fundo nos livros, autores e universos publicados pela Ediouro.</p><div class="discover-filter-row"><button class="discover-filter active" data-kind="todos" onclick="discoverFilter('todos')">Tudo</button>${kinds.map(k=>`<button class="discover-filter" data-kind="${esc(k)}" onclick="discoverFilter('${esc(k)}')">${esc(k)}</button>`).join('')}</div></div></div><section><div class="wrap"><div class="discover-lead-v3"><article class="discover-lead-card" onclick="go('/descubra/${featured.slug}')"><div><div class="eyebrow" style="color:#aaa0d8">${esc(featured.kind)}</div><h2>${esc(featured.title)}</h2><p>${esc(featured.standfirst)}</p><div class="discover-lead-meta">${discoverMeta(featured)}</div><div class="cta">Ler conteúdo</div></div>${discoverPostVisual(featured)}</article><div class="discover-side-v3">${rest.map(p=>{const b=discoverPostBrand(p);const books=discoverPostBooks(p).slice(0,3);return `<article class="discover-story-card" onclick="go('/descubra/${p.slug}')">${b?`<div class="discover-brand"><span class="discover-brand-dot" style="background:${b.color}"></span>${esc(b.name)}</div>`:''}<div class="eyebrow">${esc(p.kind)}</div><h3>${esc(p.title)}</h3><p>${esc(p.standfirst)}</p><div class="discover-lead-meta">${discoverMeta(p)}</div>${books.length?`<div class="discover-mini-covers">${books.map(w=>cover(w)).join('')}</div>`:''}</article>`}).join('')}</div></div></div></section><section style="background:#e9e2d7"><div class="wrap"><div class="sec-head"><div><div class="eyebrow">Por onde começar</div><h2>Escolha um caminho.</h2></div><div class="link" onclick="go('/livros')">Ver todo o catálogo</div></div><div class="discover-paths"><article class="discover-path" onclick="go('/livros?categoria=fantasia')"><div class="eyebrow" style="color:inherit;opacity:.65">Fantasia</div><div><h3>Entrar em outro mundo.</h3><p>Séries, universos e leituras para desaparecer por algumas horas.</p></div></article><article class="discover-path" onclick="go('/livros?categoria=crime')"><div class="eyebrow" style="color:inherit;opacity:.65">Crime e mistério</div><div><h3>Montar o quebra-cabeça.</h3><p>Casos, pistas e histórias que pedem mais uma página.</p></div></article><article class="discover-path" onclick="go('/livros?categoria=romance')"><div class="eyebrow" style="color:inherit;opacity:.65">Romance</div><div><h3>Sentir sem pedir licença.</h3><p>Histórias de amor, desejo, encontro e desencontro.</p></div></article><article class="discover-path" onclick="go('/series')"><div class="eyebrow" style="color:inherit;opacity:.65">Séries</div><div><h3>Quando um livro não basta.</h3><p>Ordens de leitura e universos para continuar explorando.</p></div></article></div></div></section><section><div class="wrap"><div class="sec-head"><div><div class="eyebrow">Arquivo editorial</div><h2>Todos os conteúdos</h2></div></div>${POSTS.length?`<div class="discover-all-grid">${POSTS.map(p=>{const b=discoverPostBrand(p);return `<article class="discover-all-card" data-kind="${esc(p.kind)}" onclick="go('/descubra/${p.slug}')"><div><div class="eyebrow">${esc(p.kind)}</div><h3>${esc(p.title)}</h3><p>${esc(p.standfirst)}</p></div><div><div class="discover-lead-meta">${discoverMeta(p)}</div>${b?`<div class="discover-brand" style="margin-top:14px"><span class="discover-brand-dot" style="background:${b.color}"></span>${esc(b.name)}</div>`:''}</div></article>`}).join('')}</div>`:'<div class="discover-empty">Ainda não há conteúdos publicados.</div>'}</div></section><section class="institutional"><div class="wrap"><div class="eyebrow">Ediouro</div><h2>Todo conteúdo termina em uma nova leitura.</h2><p style="max-width:680px;line-height:1.6;color:#efefff">Use o Descubra para navegar por ideias, autores e universos, e continue pelo catálogo quando alguma história chamar você.</p><div class="cta" onclick="go('/livros')">Explorar livros</div></div></section></main>`}
function articlePage(slug){const p=P[slug];if(!p)return notFound();const related=discoverPostBooks(p),im=discoverPostBrand(p);const authors=[...new Map(related.flatMap(w=>authorSlugs(w)).map(s=>[s,C[s]])).values()].filter(Boolean);const series=[...new Map(related.filter(w=>w.series&&S[w.series]).map(w=>[w.series,S[w.series]])).values()];const idx=POSTS.findIndex(x=>x.slug===slug),next=POSTS[(idx+1)%POSTS.length];return `<main><div class="article-v3-hero"><div class="wrap article-v3-grid"><div><div class="eyebrow">${esc(p.kind)}${im?' · '+esc(im.name):''}</div><h1>${esc(p.title)}</h1><p class="standfirst">${esc(p.standfirst)}</p><div class="article-v3-meta"><span>${new Date(p.publishedAt+'T12:00:00').toLocaleDateString('pt-BR',{day:'2-digit',month:'long',year:'numeric'})}</span><span>${p.readingTime} min de leitura</span></div></div>${related.length?`<div class="article-v3-covers">${related.slice(0,3).map(w=>cover(w)).join('')}</div>`:''}</div></div><section><div class="wrap article-v3-body-wrap"><article class="article-v3-copy">${p.body.map(x=>`<p>${esc(x)}</p>`).join('')}</article><aside class="article-v3-side">${im?`<div class="article-v3-side-block"><div class="eyebrow">Casa editorial</div><h4>${esc(im.name)}</h4><div class="link" onclick="go('/marcas/${im.slug}')">Conhecer a marca</div></div>`:''}${authors.length?`<div class="article-v3-side-block"><div class="eyebrow">Autores relacionados</div>${authors.map(a=>`<div class="article-v3-side-link" onclick="go('/autores/${a.slug}')">${esc(a.name)} →</div>`).join('')}</div>`:''}${series.length?`<div class="article-v3-side-block"><div class="eyebrow">Séries e universos</div>${series.map(s=>`<div class="article-v3-side-link" onclick="go('/series/${s.slug}')">${esc(s.name)} →</div>`).join('')}</div>`:''}</aside></div></section>${related.length?`<section class="related"><div class="wrap"><div class="sec-head"><div><div class="eyebrow">Continue lendo</div><h2>Os livros deste conteúdo</h2></div></div><div class="shelf">${related.map(bookCard).join('')}</div></div></section>`:''}${next&&next.slug!==slug?`<section class="article-next"><div class="wrap"><article class="article-next-card" onclick="go('/descubra/${next.slug}')"><div><div class="eyebrow" style="color:#aaa097">Próximo no Descubra</div><p>${esc(next.kind)}</p></div><div><h2>${esc(next.title)}</h2><p>${esc(next.standfirst)}</p></div></article></div></section>`:''}</main>`}